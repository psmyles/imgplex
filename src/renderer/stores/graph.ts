// Graph store — Phase 4: connect to Svelte Flow state
import type { GraphNode, GraphEdge, GraphViewport } from '../../shared/types.js'

export const defaultNodes: GraphNode[] = []
export const defaultEdges: GraphEdge[] = []
export const defaultViewport: GraphViewport = { x: 0, y: 0, zoom: 1 }
